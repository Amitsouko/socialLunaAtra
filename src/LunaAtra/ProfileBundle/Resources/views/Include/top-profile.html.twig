

<div id="cover-profile">
{% if user.cover %}
  <div id="image-cover-profile" style="background-image:url('{{ asset(user.cover.WebPath) }}');">
  {% else %}
  <div id="image-cover-profile" style="background-image:url('{{ asset('bundles/core/images/background-home/'~ random(3) ~'.jpg') }}');">
 {% endif %}
 </div>
 <div class="cover-content">
     <div id="main-profile-image">
         <a href="{{ asset(user.WebPath) }}" data-lightbox="profile" data-title="{{ user.username }}">
            <img src="{{ user.WebPath | imagine_filter('profile_thumbnail') }}">
        </a>
    </div>
    <div class="container">
        <div class="row">
            <div class="tram col-md-6 col-md-offset-3 col-lg-6 col-lg-offset-3">
                <h1 class="profile-name ">
                    <a href="{{ path("user", {"username": user.username}) }}">{{ user.username }}</a>
                       {# {% if app.user == user %} 
                            <em><a href="{{ path("fos_user_profile_edit") }}">Éditer</a></em>
                        {% endif%} 2} #}
                </h1>
                {% if user.announce %}
                <h2 class="main-announce">
                    “&thinsp;{{ user.announce }}&thinsp;”   
                </h2>
                {% endif %}
            </div>
        </div>
    </div>
 </div>

</div>{#
<div class="container">
    <div class="sub-menu-profile">
        <ul class="list-inline hidden-xs hidden-sm">
            <li>
                <a {% if path("user", {"username": user.username}) == currentPath  %}class="selected" {% endif %} 
                    href="{{ path("user", {"username": user.username}) }}"  >
                    Profil
                </a>
            </li>
            <li>
                <a {% if  path("user-bio", {"username": user.username}) == currentPath  %}class="selected" {% endif %}
                    href="{{ path("user-bio", {"username": user.username}) }}">
                    Biographie
                </a>
            </li>
            <li>
                <a {% if path("user-characters", {"username": user.username}) == currentPath  %}class="selected" {% endif %} 
                    href="{{ path("user-characters", {"username": user.username}) }}">
                    Personnages
                </a>
            </li>
            <li>
                <a {% if path("user-blog", {"username": user.username}) == currentPath  %}class="selected" {% endif %} href="{{ path("user-blog", {"username": user.username}) }}">
                    Blog
                </a>
            </li>
        
        </ul>

            <span class="hidden-md hidden-lg pull-left responsive-selected-menu">{{ pagename }}</span>
            <a  class=" mini-menu pull-right hidden-md hidden-lg dropdown" href=""><span class="glyphicon glyphicon-align-justify"></span></a>
            <ul id="profile-menu-dropdown" class="list-unstyled dropdown-menu">
               <li>
                <a {% if path("user", {"username": user.username}) == currentPath  %}class="selected" {% endif %} 
                    href="{{ path("user", {"username": user.username}) }}"  >
                    Profil
                </a>
            </li>
            <li>
                <a {% if  path("user-bio", {"username": user.username}) == currentPath  %}class="selected" {% endif %}
                    href="{{ path("user-bio", {"username": user.username}) }}">
                    Biographie
                </a>
            </li>
            <li>
                <a {% if path("user-characters", {"username": user.username}) == currentPath  %}class="selected" {% endif %} 
                    href="{{ path("user-characters", {"username": user.username}) }}">
                    Personnages
                </a>
            </li>
            <li>
                <a {% if path("user-blog", {"username": user.username}) == currentPath  %}class="selected" {% endif %} href="{{ path("user-blog", {"username": user.username}) }}">
                    Blog
                </a>
            </li>

        </ul>
            </ul>

    </div>

    <div class="clearfix"></div>
</div> #}